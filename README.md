# PHYS 3330 - Electronics for the Physical Sciences

Produced for a class at CU Boulder. Includes a library for relevant functions, prelabs, and lab writeups.

To install the library:
``` pip3 install -e ~/Path/to/folder/jlab```

then import with
```import jlab as jl```
